# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Gradle 데몬 메모리 — Hilt KSP + Compose 빌드에 2GB는 부족해서 4GB로 상향
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1g -XX:+UseParallelGC -Dfile.encoding=UTF-8

# Kotlin 컴파일러 데몬 메모리 (Gradle 데몬과 별개 프로세스)
kotlin.daemon.jvmargs=-Xmx2048m

# 빌드 캐시 — 이전 빌드 산출물 재사용
org.gradle.caching=true

# 구성 캐시 — configuration phase 결과 재사용
org.gradle.configuration-cache=true

# 병렬 빌드 — 현재 단일 모듈이라 효과는 작지만 멀티모듈 대비 켜둠
org.gradle.parallel=true

android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true